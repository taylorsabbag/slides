/* Add imports for fonts above here, syntax @import '' */
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
    --r-link-color: #3182ce;
    --r-link-color-hover: #2c5282;
}

.hljs-title,
.hljs-keyword {
    font-weight: normal;
}

.reveal pre,
.reveal code {
    font-family: 'Fira Code', monospace;
    font-size: 1.2rem;
    overflow: hidden !important;
}